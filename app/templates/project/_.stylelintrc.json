{
  "extends": [
    "stylelint-config-wordpress/scss",
    "genetiks/config"
  ],

  "rules": {
    "string-quotes": "single"
  },

  "ignoreFiles": [
    "node_modules/**",
    "assets/css/*.css"<% if ( opts.bootstrap ) { %>,
    "src/sass/_bootstrap-*.scss"<% } %>
  ]
}
