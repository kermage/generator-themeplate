{
	"extends": [
		"stylelint-config-wordpress/scss",
		"genetiks/config"
	],

	"rules": {
		"string-quotes": "single"
	}<% if ( opts.bootstrap ) { %>,

	"ignoreFiles": [
		"src/sass/_bootstrap-*.scss"
	]<% } %>
}
