<?xml version="1.0"?>
<ruleset name="<%= opts.themeName %> Standards">
	<!-- Set a description for this ruleset. -->
	<description>The code standard rules for <%= opts.themeName %>.</description>

	<!-- Include the WordPress ruleset. -->
	<rule ref="WordPress"/>
</ruleset>
